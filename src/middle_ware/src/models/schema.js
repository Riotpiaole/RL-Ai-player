import mongoose from 'mongoose';

var Schema = mongoose.Schema;
/**
 * add schema to define model
 * var schema_value = new Schema({
 *   p_id: Number,
 *   title: String,
 *   price: Number,
 *   inventory_count: Number,
 * });
 */


/**
 * recreating model by re-defining the
 *  var VARIABLE = mongoose.model( 'NAME' , schema_value )
 * */


// export { VARIABLE };
